on:
  pull_request_target:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: gh pr checkout 123
      - run: pip install .
        env:
          AWS: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
